
//set the pic number at one and store it in a variable called picNumber

//use the setInterval method, call the function slider and set the interval. Store this in the variable timer


function slider() {
  
  //use a condition to check if the picNumber is equal to the max picnumber

    //if it is...you'll need to reset something

  //else 

    //you'll need to increment something

 //build the string that will form the new img src, use concatenation!   
  
  //get the sliderPic element by id and set the .src to the new image string
    
}